# GitHub Pages helpers

- `index.html` — optional org-level redirect to `/GrowPal/` (copy to repo root only if your Pages source is the repository root, not `/docs`).

The live static preview is built into `/docs` via `npm run build:pages`.
